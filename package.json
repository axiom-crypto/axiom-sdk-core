{
  "name": "@axiom-crypto/core",
  "version": "0.3.1",
  "description": "The SDK to build on top of Axiom, the ZK Coprocessor for Ethereum.",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "rm -rf ./dist/* && tsc && cp ./package.json ./dist/.",
    "build:prod": "rm -rf ./dist/* && ENV=prod tsc && cp ./package.json ./dist/.",
    "build:staging": "rm -rf ./dist/* && ENV=staging tsc && cp ./package.json ./dist/.",
    "publish": "",
    "test": "jest",
    "test:unit": "jest test/unit",
    "dev": "ts-node src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^18.15.10",
    "dotenv": "^16.0.3",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "tslib": "^2.5.0",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "ethers": "^6.6.0",
    "graphql": "^16.6.0",
    "graphql-request": "^5.2.0",
    "merkletreejs": "^0.3.10"
  }
}
